secrets/*
.venv/
base_models
**/__pycache__/
*.pyc

# Large model checkpoint — exclude everything except the config yaml
checkpoint/
!checkpoint/temperature-run2/params-temperature-exp-2-c30.yaml

# app_data/ is pre-computed and MUST be committed (Streamlit Cloud reads it)
# (nothing to exclude here — all files are small)

# Raw climate data (never commit — too large)
data/raw/
